// No exact-style zero-stock or replenishment records have been confirmed for
// the live catalogue. Keep this empty until a dated supplier record is checked.
export const styleStock = Object.freeze({});
